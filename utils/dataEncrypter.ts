// import * as crypto from 'crypto';

// function decryptQueryParam(encryptedParam) {
//     crypto
//   const bytes = .AES.decrypt(encryptedParam, secretKey);
//   return bytes.toString(CryptoJS.enc.Utf8);
// }

// // Usage Example
// const decryptedParam = decryptQueryParam(encryptedParam);
// console.log('Decrypted:', decryptedParam);